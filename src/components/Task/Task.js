import Button from "../Button/Button"

const Tasks = () => {
    return (
        <div> This is Tasks 
            
            {/* <Button function={} value={} default={}/> */}
        </div>

    )
}

export default Tasks